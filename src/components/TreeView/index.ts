/**
 * TreeView component exports
 * 
 * Re-exports the TreeView component and its types for convenient importing.
 * This barrel export pattern allows consumers to import from the directory
 * rather than the specific file.
 */

export { TreeView } from './TreeView';
export type { TreeViewProps } from './TreeView';